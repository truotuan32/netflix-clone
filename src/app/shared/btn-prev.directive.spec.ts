import { BtnPrevDirective } from './btn-prev.directive';

describe('BtnPrevDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnPrevDirective();
    expect(directive).toBeTruthy();
  });
});
